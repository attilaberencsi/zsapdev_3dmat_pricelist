_schema-version: '3.1'
ID: threedprinterprice
version: 1.0.0
description: A simple CAP project.
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
  subdomain: sapdevcode
build-parameters:
  before-all:
    - builder: custom
      commands:
        - rm -rf build/dist
        - mkdir -p build/dist
        - rm -rf build/ui-deployer
        - mkdir -p build/ui-deployer
    - builder: custom
      commands:
        - npm install --production
        - >-
          bash -c "cds -v | grep 'cds-dk' || npm i --production --no-save
          @sap/cds-dk"
        - bash -c "export PATH=$PATH:$(pwd)/node_modules/.bin"
        - cds build --production
  after-all:
    - builder: custom
      commands:
        - npm install
modules:
  - name: threedprinterprice-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: threedprinterprice-service-uaa
      - name: threedprinterprice-service-db
  - name: threedprinterprice-srv
    type: nodejs
    path: gen/srv
    parameters:
      memory: 256MB
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    build-parameters:
      builder: npm
      ignore:
        - node_modules
        - package-lock.json
    requires:
      - name: threedprinterprice-service-uaa
      - name: threedprinterprice-service-db
      - name: threedprinterprice-service-destination
      - name: threedprinterprice-service-metadata
      - name: threedprinterprice-service-connectivity
      - name: threedprinterprice-service-logs
  - name: threedprinterprice-aux-destinations
    type: com.sap.application.content
    parameters:
      content:
        subaccount:
          existing_destinations_policy: update
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: threedprinterprice-service-uaa
              ServiceInstanceName: threedprinterprice-uaa
              ServiceKeyName: threedprinterprice-uaa-key
              sap.cloud.service: lcap.threedprinterprice
            - Authentication: OAuth2UserTokenExchange
              Name: threedprinterprice-app-srv
              TokenServiceInstanceName: threedprinterprice-uaa
              TokenServiceKeyName: threedprinterprice-uaa-key
              URL: ~{srv-api/srv-url}
              sap.cloud.service: lcap.threedprinterprice
            - Name: threedprinterprice-service-html5-apps-repo-host
              ServiceInstanceName: threedprinterprice-html5-apps-repo-host
              ServiceKeyName: threedprinterprice-html5-apps-repo-host-key
              sap.cloud.service: lcap.threedprinterprice
            - Authentication: OAuth2UserTokenExchange
              Name: threedprinterprice-priceService
              TokenServiceInstanceName: threedprinterprice-uaa
              TokenServiceKeyName: threedprinterprice-uaa-key
              URL: ~{srv-api/srv-url}/service/threedprinterpriceSvcs
              sap.cloud.service: lcap.threedprinterprice
              sap.applicationdevelopment.actions.enabled: false
              sap.build.usage: CAP
              sap.processautomation.enabled: false
              HTML5.DynamicDestination: false
              WebIDEEnabled: false
              MobileEnabled: false
              AppgyverEnabled: false
    build-parameters:
      no-source: true
    requires:
      - name: threedprinterprice-service-uaa
        parameters:
          service-key:
            name: threedprinterprice-uaa-key
      - name: srv-api
      - name: threedprinterprice-service-html5-apps-repo-host
        parameters:
          service-key:
            name: threedprinterprice-html5-apps-repo-host-key
      - name: threedprinterprice-service-destination
        parameters:
          content-target: true
  - name: threedprinterprice-launchpad
    type: html5
    path: build/launchpad
    build-parameters:
      builder: custom
      commands:
        - npm install
        - >-
          npx -p @ui5/cli@^3.0.0 ui5 build --dest dist
          --include-task=generateCachebusterInfo
        - bash -c "cd dist && npx bestzip ../../../build/dist/launchpad.zip *"
        - rm -rf dist
      _artifactName: launchpad.zip
      supported-platforms: []
      build-result: ../../build/dist
  - name: threedprinterprice-aux-ui-deployer
    type: com.sap.application.content
    path: build/ui-deployer
    build-parameters:
      build-result: resources
      requires:
        - name: threedprinterprice-launchpad
          artifacts:
            - launchpad.zip
          target-path: resources
    requires:
      - name: threedprinterprice-service-html5-apps-repo-host
        parameters:
          content-target: true
resources:
  - type: org.cloudfoundry.managed-service
    name: threedprinterprice-service-uaa
    parameters:
      service: xsuaa
      service-plan: application
      service-name: threedprinterprice-uaa
      path: xs-security.json
      config:
        oauth2-configuration:
          redirect-uris:
            - https://${subdomain}.launchpad.${default-domain}/login/*
            - https://${subdomain}.${default-domain}/login/*
          credential-types:
            - binding-secret
            - x509
        xsappname: threedprinterprice-${space}
        role-collections: []
  - type: org.cloudfoundry.managed-service
    name: threedprinterprice-service-db
    parameters:
      service: hana
      service-plan: hdi-shared
      service-name: threedprinterprice-db
  - type: org.cloudfoundry.managed-service
    name: threedprinterprice-service-destination
    parameters:
      service: destination
      service-plan: lite
      service-name: threedprinterprice-destination
      config:
        HTML5Runtime_enabled: true
        version: 1.0.0
  - type: org.cloudfoundry.user-provided-service
    name: threedprinterprice-service-metadata
    parameters:
      service: metadata
      service-plan: standard
      service-name: threedprinterprice-metadata
      config:
        default-domain: ${default-domain}
  - type: org.cloudfoundry.managed-service
    name: threedprinterprice-service-connectivity
    parameters:
      service: connectivity
      service-plan: lite
      service-name: threedprinterprice-connectivity
  - type: org.cloudfoundry.managed-service
    name: threedprinterprice-service-logs
    parameters:
      service: application-logs
      service-plan: lite
      service-name: threedprinterprice-logs
  - type: org.cloudfoundry.managed-service
    name: threedprinterprice-service-html5-apps-repo-host
    parameters:
      service: html5-apps-repo
      service-plan: app-host
      service-name: threedprinterprice-html5-apps-repo-host
